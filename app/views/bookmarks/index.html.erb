<%= render 'boards/search_form', q: @q, url: boards_path %>
  <!-- 掲示板一覧 -->
  <div class="flex flex-wrap">
    <% if @boards.present? %>
      <%= render @boards %>
    <% else %>
      <div class="mb-3">掲示板がありません</div>
    <% end %>
  </div>
  <%= paginate @boards %>